extends layout

block sidebar
    ul(class='sidebar-nav')
        li
            a(href='/') Home
        li
            a(href='/cooks') All cooks

block content
    h2= message + ' ' + id
    if flag
        form(method='POST' action='/cooks/' + id + '/delete')
            div.form-group
                input(type='submit', value='delete')